---
schema_type: data_dictionaries
schema_name: access
description: Access CDM data dictionary bundle
repository: ftd-schema
owner: ftd

versions:
  - tag: "v1.0.0"
    directory: "v1.0.0"
    released: "2026-06-17"
    status: stable
    breaking_changes: []
    notes: |
      - Initial release of include access after refactor related to the common-data-model.
      - This release includes include access data dictionaries and commonDM dds.
      - include-access-model repo is not setup with the release artifacts at this time, therefore, the data dictionaries were placed manually.
    deprecated: false

compatibility:
  tools:
    - include-dbt-sandbox

